<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Hexagon.DesktopShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Hexagon"
    Title="Hexagon"
    FlyoutBehavior="Locked"
    FlyoutWidth="64">

    <Shell.FlyoutBackground>
        <LinearGradientBrush StartPoint="0,0.1" EndPoint="0,1">
            <GradientStop 
                Color="{AppThemeBinding Light={StaticResource GradientStart}, Dark={StaticResource GradientStartDark}}"
                Offset="0"/>
            <GradientStop 
                Color="{AppThemeBinding Light={StaticResource GradientEnd}, Dark={StaticResource GradientEndDark}}"
                Offset="1"/>
        </LinearGradientBrush>
    </Shell.FlyoutBackground>

    <FlyoutItem
    Icon="today.png"
    Title="Teď">
        <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    </FlyoutItem>
    <FlyoutItem
    Icon="settings.png"
    Title="Nastavení">
        <ShellContent
        ContentTemplate="{DataTemplate local:User}"
        Route="User" />
    </FlyoutItem>

</Shell>
